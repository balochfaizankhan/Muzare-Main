rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /workspaces/{workspaceId}/{file=**} {
      allow read, write: if request.auth != null && request.auth.uid == workspaceId;
    }

    match /{file=**} {
      allow read, write: if false;
    }
  }
}
